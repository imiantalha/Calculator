{
  "name": "calculator",
  "private": true,
  "version": "2.0.0",
  "description": "Production-style calculator with Playwright end-to-end tests",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
